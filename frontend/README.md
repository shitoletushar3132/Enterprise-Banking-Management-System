# Frontend package documentation

React + Vite SPA for EBMS.

## Local commands

```bash
cp .env.example .env
npm run dev
npm run build
```

Dev server: `http://localhost:5173` (proxies `/api` to the NestJS backend).
