# Backend package documentation

See repository `docs/` for product design documents.

## Local commands

```bash
cp .env.example .env
npm run start:dev
npm test
npm run test:e2e
```

API prefix: `/api/v1`  
Swagger: `/api/docs`
